	ТИПЫ ДАННЫХ В JS

1. Простые типы:
- числовой 1,2,3;
- строчный 'string', 'name';
- логический тип (boolean):
true or false;
- null (когда элемента вообще не существует);
- undefined (когда элемент существует, но не имеет значения);
- Symbol().

2. Объекты:
 1)  Специальные объекты:
     Массивы:
	let photo = ['white.png', 'grey.png', 'black.png'] //Массив используется для хранения ЛЮБЫХ типов данных! 
	
	console.log(photo)

	console.log(photo[2]) //Каждый элемент в массиве имеет свой порядковый номер. Исчисление начинается с 0!
	console.log(photo[0])
	console.log(photo[1])

     Функции function();
     Объект Даты;
     Регулярные выражения;
     Ошибки.
 2) Обычные объекты.